from setuptools import setup

# All runtime dependencies (scikit-learn, roma, gradio, scipy, einops, etc.)
# are provided by the pixi environment. curope and asmk are installed as
# separate pixi pypi-dependencies. No install_requires needed.
setup()
