[project]
name = "sapiens2-pose"
version = "0.1.0"
authors = [{ name = "pablo vela", email = "pablovela5620@gmail.com" }]
requires-python = ">=3.12"
dependencies = [
    # common: numpy, scipy, jaxtyping, rerun-sdk, gradio, gradio-rerun,
    #         huggingface_hub, hf-transfer, tqdm, pyserde, av, opencv, transformers
    # cuda: pytorch-gpu, torchvision
    "safetensors",
    "timm",
    "tyro>=0.9.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sapiens2_pose"]

[tool.ruff]
exclude = ["src/sapiens2_pose/assets", "src/sapiens2_pose/sapiens_lite"]
line-length = 150

[tool.ruff.lint]
select = ["E", "F", "UP", "B", "SIM", "I"]
ignore = ["E501", "F722", "F821", "UP037", "UP040"]

[tool.vulture]
paths = ["src/sapiens2_pose", "tests", "tools"]
exclude = ["*/.pixi/*", "*/__pycache__/*", "*/build/*", "*/dist/*", "*/node_modules/*"]
sort_by_size = true
# Ignore low-confidence public APIs and vendored model hooks; keep high-confidence findings actionable.
min_confidence = 80

[tool.pytest.ini_options]
markers = [
    "integration: needs a GPU, a downloaded asset/model, the catalog server, a device, or network (run: pixi run -e sapiens2-pose-dev tests-integration)",
    "golden: reference comparison against stored ground truth (run: pixi run -e sapiens2-pose-dev tests-golden)",
]
addopts = "-m 'not integration and not golden'"
